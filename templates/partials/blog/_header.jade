.small-10.small-centered.blog_title
  h1 Blog
.small-12.blog_categories
  - var tags = env.helpers.getAllTags();
  ul
    each tag in tags
      li
        a(href="#{locals.path}/blog/tag/#{tag}/1/") #{tag}
      