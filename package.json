{
  "version": "2.0.1",
  "scripts": {
    "setup": "npm install && bower install",
    "build": "grunt build && wintersmith build --clean -c config.json && grunt copy:htaccess && grunt copy:html",
    "deploy": "npm run build && s3cmd put -r www/ s3://flipbase.com"
  },
  "dependencies": {
    "grunt": "*",
    "grunt-contrib-clean": "*",
    "grunt-contrib-copy": "*",
    "grunt-contrib-cssmin": "*",
    "grunt-contrib-sass": "*",
    "grunt-contrib-uglify": "*",
    "grunt-contrib-watch": "*",
    "grunt-modernizr": "^1.0.2",
    "load-grunt-tasks": "*",
    "moment": "2.3.x",
    "typogr": "0.5.x",
    "underscore": "1.4.x",
    "wintersmith-tag": "^1.0.2"
  },
  "devDependencies": {
    "grunt-assets-versioning": "^1.0.6",
    "wintersmith-livereload": "^0.2.2"
  }
}
